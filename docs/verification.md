# 검증 기록

검증일: 2026-09-24. 원본 기준 커밋: `d444acca440e19b22d0ab7064fba5eed9b330e65`.

## 실행한 검사

| 범위 | 결과 | 확인한 내용 |
| --- | --- | --- |
| 공개 화면 DOM 로직 | 41개 통과 | 앨범 18장 단위 페이지, 사진 확대·이전·다음, 배경 스크롤 잠금·복원, 앨범에 한정한 복사 제한, 무작위 기본 카드, 5개 중복 없는 후보, 재추첨 후 선택 초기화, 선택한 캐릭터 저장, 날짜·주소·지도 링크 |
| 캐릭터·정원 경계 조건 | 위 41개에 포함 | 후보 0·1·3개, 깨진 이미지 제외와 대체, 후보 300개, 정원 8명의 6+2 페이지 접근 |
| PostgreSQL 접근 권한 | 35개 통과 | 익명·일반 로그인·관리자 구분, 공개/숨김 조회, 직접 입력 차단, 등록 입력 길이, 요청 재시도 중복 방지, 이전 캐릭터 보존, Storage 공개 조건, 참조 파일 삭제 방지, 기존 메시지 가져오기와 중복 충돌, 스키마 재실행 |
| 연결·관리자 화면 로직 | 9개 통과 | 포함된 Supabase SDK의 공개 키 헤더와 관리자 JWT 유지, 미설정 안내, 일반 로그인 거절, 관리자 편집 항목 |
| 원본 보존 | 통과 | 이름·예식일·예식장·부모님·주소·교통·마무리 문구 8개 블록의 HTML 비교. 사진·신랑 신부·정원·원본 캐릭터·음악은 원본 내장 자료와 바이트 일치 |
| 파일·문법 | 통과 | HTML/CSS의 로컬 경로 19개 존재, 이미지 11개 디코딩, 새 캐릭터 6개의 실제 투명 알파, 작성한 JavaScript의 문법 검사 |

합계 85개의 실행 검증 항목이 통과했습니다. 마지막 파일·원본 보존 검사는 이 숫자와 별도입니다.

DOM 검사는 JSDOM에서 실제 애플리케이션 스크립트를 실행했습니다. 이미지·애니메이션·브라우저 대화상자 일부를 대체하므로 실제 화면 렌더링이나 터치 검증을 뜻하지 않습니다. PostgreSQL 검사는 PGlite와 테스트용 Auth/Storage 테이블에서 실행했으며, 실제 Supabase 서비스의 로그인·파일 API를 연결한 검사는 아닙니다. SDK 통신 검사는 모의 응답을 사용했습니다.

## 아직 확인하지 못한 항목

- 브라우저 검증 환경에서 로컬 파일 열기가 차단되어 수정 화면의 시각 검사와 직접 클릭 검사는 수행하지 못했습니다. iPhone Safari / Android Chrome 실제 단말 검사도 미실시입니다.
- Supabase 프로젝트 URL·publishable key가 아직 없습니다. 실제 관리자 로그인, 여러 장 업로드, 다른 휴대폰에서 동일한 메시지 조회, 실제 Storage 서명 URL 발급은 설정 후 확인해야 합니다.
- 카카오 지도 JavaScript 키가 아직 없습니다. 실제 SDK 로드·주소 검색·지도 마커·드래그·확대는 미확인입니다. 미설정 상태의 주소와 외부 지도 링크는 유지했습니다.
- 원본 Apps Script 방명록은 이 환경의 읽기 요청에서 403을 반환했습니다. 원본 서버와 시트는 변경하지 않았으며, 기존 메시지 전체를 가져왔다고 볼 수 없습니다.
- 원본 앨범의 실사진은 1장입니다. 19장 이상 페이지 검사는 테스트 자료로 확인했습니다.

## 연결 후 확인

[`README.md`](../README.md)의 ‘처음 연결한 뒤 확인하는 순서’에 따라 두 기기 간 메시지 공유, 공개/숨김 반영, 실제 지도, 모바일 확대 사진의 닫기와 스크롤 복원을 확인합니다. 설정 전 저장한 메시지는 해당 브라우저의 로컬 기록이며 다른 기기에 공유되지 않습니다.

재실행 방법과 테스트 의존성은 README에 적었습니다. 테스트 패키지는 사이트 운영에 필요하지 않습니다.
